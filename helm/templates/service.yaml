apiVersion: v1
kind: Service
metadata:
  name: {{ include "elsa.fullname" . }}
  labels:
    {{- include "elsa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "elsa.selectorLabels" . | nindent 4 }}
