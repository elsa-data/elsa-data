backend/node_modules/
backend/coverage/

# ignore all files generated by the EdgeDb tooling - we will generate these
# inside the Docker image
backend/dbschema/edgeql-js/
backend/dbschema/interfaces.ts

frontend/node_modules/
frontend/build/

common/**/node_modules/

# because we are doing a copy . . we need to explicitly ignore things that we don't want to trigger a rebuild
Dockerfile
.dockerignore
