{
  // note: this is *not* the same content as test1.json5 in folder1 - this is so we can test folder ordering
  stringValue: "still a string",
  superAdmins: [
    {
      sub: "http://test3",
    },
  ],
}
