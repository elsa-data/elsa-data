doctype html
html
    body
        p
            | Hi #{name},
            |
            | #{notification}
            |
            if (typeof(time) !== "undefined")
                | Time
                | #{time}
                |
            | - #{fromName}
